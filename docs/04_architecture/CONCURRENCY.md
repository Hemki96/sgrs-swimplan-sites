# Gleichzeitiges Bearbeiten
MVP nutzt Entity-`version`. Update liefert `expectedVersion`. Bei Abweichung wird nicht überschrieben; UI fordert Reload/erneutes Anwenden. Später optional feldbasierter Merge/Realtime.
