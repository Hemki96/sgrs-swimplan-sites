# Private Sites Preview Prompt

Erzeuge eine private ChatGPT-Sites-Vorschau aus dem Repository. Veröffentliche nicht öffentlich. Prüfe zuerst die tatsächlich verfügbare persistente Storage-Runtime und dokumentiere Bindings/APIs per ADR. Keine API-Namen erfinden.
